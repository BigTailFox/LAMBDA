"""ZCM type definitions
This file automatically generated by zcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class Started(object):
    __slots__ = ["timestamp"]

    IS_LITTLE_ENDIAN = False;
    def __init__(self):
        self.timestamp = 0

    def encode(self):
        buf = BytesIO()
        buf.write(Started._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">q", self.timestamp))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != Started._get_packed_fingerprint():
            raise ValueError("Decode error")
        return Started._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = Started()
        self.timestamp = struct.unpack(">q", buf.read(8))[0]
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if Started in parents: return 0
        tmphash = (0x1057379c873b1c2c) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + ((tmphash>>63)&0x1)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if Started._packed_fingerprint is None:
            Started._packed_fingerprint = struct.pack(">Q", Started._get_hash_recursive([]))
        return Started._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

